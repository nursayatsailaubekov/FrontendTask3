What is JSX?

JSX is a syntax extension for JavaScript used in React. It allows writing HTML-like code inside JavaScript. It makes UI development more readable and easier to manage.

3 Rules of JSX:

Must return one root element.

All tags must be closed.

Use className instead of class.

Example of Embedded Expression:

<p>5 + 5 = {5 + 5}</p>